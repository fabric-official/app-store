# --- Publishing identity & policy (used by scripts and verification tooling) ---
PUBLISH_REPO=github.com/fabric-official/app-store
ALLOWED_BRANCH=main
ALLOWED_WORKFLOW=.github/workflows/validate-and-publish.yml

# --- Registry & plugins layout ---
REGISTRY_FILE=registry/index.json
PLUGINS_DIR=plugins

# --- Sigstore / cosign (keyless) ---
COSIGN_OIDC_ISSUER=https://token.actions.githubusercontent.com
REKOR_URL=https://rekor.sigstore.dev
FULCIO_URL=https://fulcio.sigstore.dev

# --- Validation toggles ---
SCHEMA_STRICT=1            # fail PRs on any schema drift
REQUIRE_SBOM=1             # fail PRs if SBOM missing
SECRET_SCAN=1              # fail PRs on suspicious secrets in bundles

# --- SBOM tool (informational for local runs; CI uses whatever is installed) ---
SBOM_TOOL=cyclonedx
